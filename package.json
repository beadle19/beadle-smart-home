{
  "name": "beadle-smart-home",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.16.2",
    "python-shell": "^0.5.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
